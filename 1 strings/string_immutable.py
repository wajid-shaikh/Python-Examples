# strings are immutable
# immutable means once we create a string then we cant change it example

string = "string"
# string[1] = "T" # we cant change 't' as 'T' it is called immutable

# if i use replace() method
print(string.replace('t' , 'T')) #this will generate new string o/p = sTring 
print(string)